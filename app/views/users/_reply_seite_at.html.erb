
<div id="reply_field_at">

  <div id="tweeter_name_groß">&nbsp;@<%=h @user.seen_person %> </div>
  <div id="close_at">
            <%= link_to_remote image_tag('../images/close.png'),
                  :url => {:action => "close_at", :id => @user.id}
            %>
  </div>
  <div id="reply_text">
    <%= f.text_area :reply_text_at, :cols => 56, :rows => 4, :maxlength => 140, :value => "@#{@user.seen_person}" %>
  </div>

    <div id="charcount"> <span id="char-counta">140</span></div>

    <div id="reply_button_change_at" style="display: none;">
            <%= link_to_remote image_tag('../images/Tweetbutton_aktiv.png',
                :mouseover => '../images/Tweetbutton_mouseover.png'),
                :loading  => "$('tweet_sent').show(); $('tweet_sent').fade()",
                :url => {:action => "close_at", :id => @user.id}
            %>
  </div>
</div>

