<div id="reply_field">
    <div id="tweeter_name_groß">&nbsp;<%= @user.displayed_person1 %>
    </div>
  <div id="close">
    <%= link_to_remote image_tag('../images/close.png'),
          :url => {:action => "close", :id => @user.id}
    %>
  </div>
  <div id="reply_text">

    <% if @user.reply_text.nil? %> 
      <%= f.text_area :reply_text, :cols => 56, :rows => 4, :maxlength => 140, :value => "#{@user.displayed_person1}" %>
    <% elsif @user.reply_text==""%>
        <%= f.text_area :reply_text, :cols => 56, :rows => 4, :maxlength => 140, :value => "#{@user.displayed_person1}" %>
    <% else%>
      <%= f.text_area :reply_text, :cols => 56, :rows => 4, :maxlength => 140, :value => "#{@user.reply_text}" %>
    <% end %> 
    
    
  </div>

    <div id="charcount"> <span id="char-count">140</span></div>

    <div id="reply_button_change" style="display: none;">
      <%= link_to_remote image_tag('../images/Tweetbutton_aktiv.png',
          :mouseover => '../images/Tweetbutton_mouseover.png'),
          :loading  => "$('tweet_sent').show(); $('tweet_sent').fade()",
            :url => {:action => "close", :id => @user.id}
      %>
    </div>
<%= observe_form :reply_text, :url => { :action => :save_reply_text }, :frequency => 0.25 %>
</div>




